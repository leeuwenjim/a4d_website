document.addEventListener("DOMContentLoaded", function (event) {
    gridGallery({
        selector: ".gg-box",
        darkMode: false,
        layout: "square",
        gapLength: 100,
        rowHeight: 200,
        columnWidth: 200,
    });

});

